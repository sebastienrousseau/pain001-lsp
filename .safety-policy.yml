# Safety CLI policy. pain001-lsp has a small dependency tree
# (jsonschema, pygls, lsprotocol, pain001 + its transitive deps); pin known false
# positives here with an expiry so they don't go stale.
security:
  ignore-vulnerabilities: {}
